# prisma-template
PoC of making prisma and PostgresSQL working with meteorJS

to run this example make sure you have meteor cli installed [click here to install](meteor.com/install) also be sure that you have an postgresSQL and ajust in .env

run this and be happy:

```
meteor npm i 
meteor
```
